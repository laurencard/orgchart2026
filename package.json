{
  "name": "vercel-static",
  "private": true,
  "scripts": {
    "compile": "node scripts/build.js",
    "deploy": "npm run compile && vercel",
    "deploy:prod": "npm run compile && vercel --prod"
  },
  "dependencies": {
    "@babel/core": "^7.25.9",
    "@babel/preset-react": "^7.25.9",
    "@vercel/blob": "^2.6.1"
  },
  "devDependencies": {
    "vercel": "^39.0.0"
  }
}
